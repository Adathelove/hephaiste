{
    "name": "hephaiste",
    "version": "1.0.0",
    "main": "index.js",
    "bin": {
        "hephaiste": "./index.js"
    },
    "scripts": {
        "clean": "rm -rf node_modules",
        "purge": "npm cache clean --force && rm -rf node_modules package-lock.json",
        "rebuild": "npm run purge && npm install",
        "rtest": "npm run purge && npm install && npm test",
        "test": "vitest --run"
    },

    "keywords": [],
    "author": "",
    "license": "ISC",
    "description": "",
    "devDependencies": {
        "vitest": "^3.2.4"
    }
}
